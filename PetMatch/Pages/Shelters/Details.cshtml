@page
@model PetMatch.Pages.Shelters.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Shelter</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Shelter.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Shelter.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Shelter.Location)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Shelter.Location)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Admin"))
    {
        <a asp-page="./Edit" asp-route-id="@Model.Shelter.ID">Edit</a> 
        <span> | </span>
    }
    <a asp-page="./Index">Back to List</a>
</div>
